 git add -u       =>Ye kya karega:
                    ✅ Modified tracked files add karega
                    ❌ Untracked files (.idea, .iml) ko touch nahi karega

  git commit --amend --no-edit  => same commit ko override karta hai

   git commit --force  ^ iske baad use karo